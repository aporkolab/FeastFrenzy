# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.

# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/

# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~

# Various IDEs
.project
.idea/
*.tmproj
.vscode/

# OS generated
.DS_Store
Thumbs.db

# Documentation (optional - remove if you want docs in package)
# *.md

# Test files
tests/
test/
*_test.yaml

# CI/CD
.github/
.gitlab-ci.yml
.travis.yml
Jenkinsfile
azure-pipelines.yml

# Values override files (these are user-specific)
values-*.yaml
!values.yaml

# Helm chart dependencies (downloaded separately)
charts/

# Chart lock file (regenerated)
Chart.lock
