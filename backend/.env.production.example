# Railway Production Environment Variables
# ==========================================
# 
# Railway automatically sets these when you link services:
# - DATABASE_URL    (MySQL connection string from Railway MySQL addon)
# - REDIS_URL       (Redis connection string from Railway Redis addon)  
# - PORT            (Dynamic port assigned by Railway)
#
# ==========================================
# MANUALLY SET THESE IN RAILWAY DASHBOARD:
# ==========================================

# JWT Configuration (REQUIRED - change these!)
JWT_SECRET=production-secret-min-32-chars-CHANGE-THIS-NOW
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Environment
NODE_ENV=production

# Frontend URL for CORS (update after frontend deploy)
FRONTEND_URL=https://your-frontend.up.railway.app

# Optional: Override individual DB settings (not needed if DATABASE_URL is set)
# DB_HOST=
# DB_PORT=3306
# DB_NAME=
# DB_USER=
# DB_PASSWORD=
# DB_SSL=true

# Optional: Override individual Redis settings (not needed if REDIS_URL is set)
# REDIS_HOST=
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Optional: Feature flags
CACHE_ENABLED=true
DB_LOGGING=false

# Optional: Performance tuning
DB_POOL_MAX=10
DB_POOL_MIN=2
SLOW_QUERY_THRESHOLD=100

# Optional: Error tracking (Sentry)
# SENTRY_DSN=your-sentry-dsn
